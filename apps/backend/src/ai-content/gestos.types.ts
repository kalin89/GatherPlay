export const MIN_GESTURE_WORDS = 1;
export const MAX_GESTURE_WORDS_PER_REQUEST = 50;

// A diferencia de Trivia no hay opciones ni respuesta correcta que acompañar
// — una palabra o frase corta alcanza.
export type GestureWord = string;
